<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.technica2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".content_assignments"
            android:label="@string/title_activity_content_assignments"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".content_name"
            android:label="@string/title_activity_content_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".content_school"
            android:label="@string/title_activity_content_school"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".content_ask"
            android:label="@string/title_activity_content_ask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>